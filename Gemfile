source "https://rubygems.org"

# Pinned to the last Jekyll 4.2.x release, which still supports Ruby 2.6
# (this machine's working system Ruby). Swap for the `github-pages` gem later
# if/when deploying for real, to match GitHub's exact build environment.
gem "jekyll", "~> 4.2.0"
